<%= form_with(model: component_category, local: true) do |form| %>
  <% if component_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(component_category.errors.count, "error") %> prohibited this component_category from being saved:</h2>

      <ul>
      <% component_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :component_category_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :component_category_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
